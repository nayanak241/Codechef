#include <bits/stdc++.h>

using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while (t--) {
        int c, x, y;
        cin >> c >> x >> y;
        int p = (c - x) * y;
        cout << p << endl;
    }
    return 0;
}
